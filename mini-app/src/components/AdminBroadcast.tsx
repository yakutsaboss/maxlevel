interface AdminBroadcastProps {
  credentials: string;
}

export function AdminBroadcast({ credentials }: AdminBroadcastProps) {
  return (
    <div className="text-telegram-hint text-center py-8">Broadcast — Task 4</div>
  );
}
